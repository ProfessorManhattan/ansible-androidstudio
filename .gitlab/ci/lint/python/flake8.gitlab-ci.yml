---
lint:flake8:
  stage: lint
  image:
    name: megabytelabs/flake8:slim
    entrypoint: ['']
  rules:
    - if: $DO_CI == 'false'
      when: never
    - exists:
        - '**/*.py'
  variables:
    GIT_DEPTH: 1
  script:
    - set -eo pipefail
    - flake8 .
