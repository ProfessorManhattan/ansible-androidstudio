---
lint:flake8:
  image:
    name: megabytelabs/flake8:slim
    entrypoint: ['']
  rules:
    - exists:
        - '**/*.py'
  script:
    - set -eo pipefail
    - flake8 .
  stage: lint
  variables:
    GIT_DEPTH: 1
