---
lint:mypy:
  stage: lint
  image:
    name: megabytelabs/yamllint:slim
    entrypoint: ['']
  rules:
    - exists:
        - '**/*.py'
  variables:
    GIT_DEPTH: 1
  script:
    - set -eo pipefail
    - yamllint -c .config/yamllint.yml -s .
