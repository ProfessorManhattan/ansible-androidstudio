---
lint:yamllint:
  image:
    name: megabytelabs/yamllint:slim
    entrypoint: ['']
  rules:
    - exists:
        - '**/*.{yaml,yml}'
  script:
    - set -eo pipefail
    - yamllint -c .config/yamllint.yml -s .
  stage: lint
  variables:
    GIT_DEPTH: 1
