---
lint:markdown:
  stage: lint
  image: pipelinecomponents/markdownlint
  rules:
    - if: $DO_CI == 'false'
      when: never
    - exists:
        - '**/*.md'
  variables:
    GIT_DEPTH: 1
  script:
    - set -eo pipefail
    - mdl .
