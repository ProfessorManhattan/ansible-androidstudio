---
lsif:javascript:
  allow_failure: true
  artifacts:
    reports:
      lsif: dump.lsif
  rules:
    - if: $DO_CI == 'false'
      when: never
  image: sourcegraph/lsif-node:latest
  needs: []
  script:
    - lsif-tsc **/*.js --AllowJs --checkJs
  stage: lsif
