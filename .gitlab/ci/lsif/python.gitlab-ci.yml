---
lsif:python:
  allow_failure: true
  artifacts:
    reports:
      lsif: dump.lsif
  rules:
    - if: $DO_CI == 'false'
      when: never
  image: megabytelabs/lsif-python:latest
  needs: []
  script:
    - lsif-py .
  stage: lsif
