---
- hosts: localhost
  roles:
    - role: '[[ The &#x60;meta/main.yml&#x60; file needs to be setup. The &#x60;galaxy_info.namespace&#x60; value is missing. ]].[[ The &#x60;meta/main.yml&#x60; file needs to be setup. The &#x60;galaxy_info.role_name&#x60; value is missing. ]]'
