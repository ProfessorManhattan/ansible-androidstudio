[defaults]
ansible_managed = "Managed by Install Doctor via Ansible (https://install.doctor)"
command_warnings = False
inventory = inventory
roles_path=~/.ansible/roles:/usr/share/ansible/roles:/etc/ansible/roles:../
